"""Language-specific post-passes.

Where a language's normalization can't be expressed as a simple
dictionary lookup (gender concordance, apocope, agreement rules), it
lives here as its own module. Spanish has the only non-trivial case so
far.
"""
